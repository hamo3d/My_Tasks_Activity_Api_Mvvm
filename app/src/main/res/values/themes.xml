<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyTasksActivity" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyTasksActivity" parent="Base.Theme.MyTasksActivity" />


    <style name="Theme.App.Launch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/transparent</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/task_app</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MyTasksActivity</item>
    </style>
</resources>